# 実装3

```sql
-- 1. （可能であれば）最も頻度高く発生するスロークエリをインデックスで高速化
-- 課題中スロークエリを吐くために「SELECT * FROM salaries;」が頻出していたのでインデックス不可

-- 2. （可能であれば）最も実行時間が長いスロークエリをインデックスで高速化
-- 同じく「SELECT * FROM salaries;」が最長だったのでインデックス不可

```